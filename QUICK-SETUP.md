# ⚡ 快速 GitHub 設置

我已經為你打開了 GitHub 創建 repository 的頁面。

## 📝 在 GitHub 上填寫：

**Repository name:** `iris-immersive-translate`

**Description:**
```
🌐 Privacy-first immersive translation using local Ollama AI models | 基於本地 AI 的隱私優先沉浸式翻譯
```

**設置：**
- ✅ Public（公開）
- ❌ **不要**勾選 "Add a README file"
- ❌ **不要**勾選 ".gitignore"
- ❌ **不要**勾選 "Choose a license"

然後點擊 **"Create repository"**

---

## 🚀 創建後，執行以下命令：

### 如果你的 GitHub username 是 `YOUR_USERNAME`，執行：

```bash
# 方法 1: HTTPS（推薦）
git remote add origin https://github.com/YOUR_USERNAME/iris-immersive-translate.git
git push -u origin main

# 方法 2: SSH（如果已設置 SSH key）
git remote add origin git@github.com:YOUR_USERNAME/iris-immersive-translate.git
git push -u origin main
```

**替換 `YOUR_USERNAME` 為你的實際 GitHub 用戶名！**

---

## ✅ 完成後

你的專案將在：
`https://github.com/YOUR_USERNAME/iris-immersive-translate`

## 🏷️ 後續優化（可選）

1. **添加 Topics**
   在 repository 頁面右側，點擊 ⚙️ 圖示，添加：
   - `chrome-extension`
   - `translation`
   - `ollama`
   - `ai`
   - `privacy`
   - `local-ai`

2. **添加 MIT License**
   在 repository 頁面點擊 "Add file" → "Create new file"
   - 檔案名：`LICENSE`
   - 使用模板：MIT License

3. **創建 Release**
   - 前往 "Releases" → "Create a new release"
   - Tag version: `v1.0.0`
   - Title: `v1.0.0 - Initial Release`

---

**🎯 告訴我你的 GitHub username，我可以幫你生成具體的命令！**
